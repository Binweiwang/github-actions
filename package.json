{
  "name": "app",
  "version": "1.0.0",
  "description": "API REST connected to MongoDB",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "update-changelog": "npx conventional-changelog -p angular -i CHANGELOG.md -s"
  },
  "author": "Binwei Wang",
  "license": "ISC",
  "dependencies": {
    "conventional-changelog-cli": "^4.1.0"
  }
}
